<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hxw.wscrm.sys.mapper.SysUserMapper">
    <insert id="saveUserAndRole">
        insert into sys_user_role(user_id,role_id) values (#{userId},#{roleId})
    </insert>

    <delete id="deleteRoleByUserId">
        delete from sys_user_role where user_id = #{userId}
    </delete>
    <select id="selectRoleIdsByUserId" resultType="java.lang.Integer">
        select role_id from sys_user_role where user_id = #{userId}
    </select>

    <select id="selectUsersByRoleId" resultType="com.hxw.wscrm.sys.entity.SysUser">
        select u.* from sys_user u
        left join sys_user_role sur on u.user_id = sur.user_id
        where sur.role_id = #{roleId}
    </select>
</mapper>
