<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hxw.wscrm.sys.mapper.SysMenuMapper">

    <select id="canBeDeleted" resultType="java.lang.Integer">
        SELECT SUM(t1.num) FROM (
                                    SELECT COUNT(1) num FROM sys_menu WHERE parent_id = #{menuId}
                                    UNION ALL
                                    SELECT COUNT(1) num FROM sys_role_menu WHERE menu_id = #{menuId}
                                ) t1
    </select>
    <select id="queryMenuIdByRoleId" resultType="java.lang.Integer">
SELECT menu_id FROM sys_menu WHERE parent_id != 0 AND menu_id IN
       ( SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId})
    </select>
    <select id="selectShowMenuParent" resultType="com.hxw.wscrm.sys.entity.SysMenu">
        select * from sys_menu where  parent_id = 0 and menu_id in (
            select menu_id from sys_role_menu where  role_id in(
                select role_id from sys_user_role where user_id in(
                    select user_id from sys_user where  username = #{userName}
                    )
                )
            )
    </select>
    <select id="selectShowMenuSubByUserName" resultType="com.hxw.wscrm.sys.entity.SysMenu">
        select * from sys_menu where  parent_id = #{menuId} and menu_id in (
            select menu_id from sys_role_menu where  role_id in(
                select role_id from sys_user_role where user_id in(
                    select user_id from sys_user where  username = #{userName}
                )
            )
        )
    </select>
</mapper>
